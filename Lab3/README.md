# Lab 3

- `bin/library_system.dart` — LW3: Library Book Management System (classes, `.where`, `.fold`)
- `bin/media_store.dart` — HW3: Digital E-Commerce Media Store (abstract class, mixin, `.where`, `.fold`)

Run:

```
dart run bin/library_system.dart
dart run bin/media_store.dart
```
